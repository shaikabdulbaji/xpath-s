package a3Finding_startswith_xpath;

public class Smple {
	
	
	
	/*
	  
	  
	 * contains() - Demonstrate at http://omayo.blogspot.in/
	 
	 
	 * Purpose:
		* It is used when the value of any attribute changes dynamically.
		
		* Has the ability to find the elements with partial text
		
		* If part of the attribute value is changing dynamically i.e. 
		  id='123main123' to id='456main456', we can use //tagName[contains(@id,'main')] to 
		  locate such dynamically changing attribute values.
		  
		* Find the input tag having the text 'ra' inside its value attribute text 
		  - //input[contains(@value,'ra')]
		  
		* Find the p tag containing the text 'Automation'  - //p[contains(text(),'Automation')]
		
		* Find the p tag containing the text 'Automation' using.- //p[contains(.,'Automation')]
	   
	   
	 */

}
